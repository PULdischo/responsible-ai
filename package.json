{
  "name": "responsible-ai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:notebooks": "python scripts/build_notebooks.py",
    "build": "npm run build:notebooks && npx @11ty/eleventy",
    "start": "npm run build:notebooks && npx @11ty/eleventy --serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "@11ty/eleventy-plugin-inclusive-language": "^1.0.3",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.2.0",
    "js-yaml": "^4.1.0",
    "luxon": "^3.1.1",
    "pagefind": "^1.3.0"
  }
}
